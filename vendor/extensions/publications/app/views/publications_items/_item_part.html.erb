<% [:locked, :unlocked].each do |ttype|%>
	<% @item.publicationitems.send(ttype).each do |item| %>
	  <%= render :partial=>"list_item_part", :locals => {:trclass => ttype.to_s, :item => item} %>
	<% end %>
<% end %>