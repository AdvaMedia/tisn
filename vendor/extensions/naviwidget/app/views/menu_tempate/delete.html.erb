<%= render :partial=>"tabs" %>
<div class="page-wrapper container">
  <table class="null-table full content-layout">
    <tr>
      <td class="header" colspan="2">
        <h1 style="background-image:url(/images/adminsystem/icons/48/content.png);">Редактирование пункта меню</h1>
      </td>
    </tr>
    <tr>
      <td class="column-center">
        <% form_for :item, :url=>{:action=>"delete", :controller=>"MenuTempate",:id=>@item.navigate.id, :tid=>@item.id} do |f| %>
        <p class="roundborder notice information">
          <i class="notice icon">&nbsp;</i>
          Если шаблон удален, то для элемента данного уровня будет использоваться родительский шаблон
        </p>
          <%= submit_tag "Я подтверждаю удаление" %>
        <% end %>
      </td>
    </tr>
  </table>
</div>