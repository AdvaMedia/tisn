<div class="page-wrapper">
  <h1>Пока тут будут показываться модули, ассоциированные с группой:</h1>
  <ul>
    <% A2mCms::ExtensionLoader.grouped_extensions["content"][1].each do |m| %>
    <li><a href="/<%= "#{m.admin_index}" || "#" %>"> <%= m.title %> </a></li>
    <% end %>
  </ul>
</div>